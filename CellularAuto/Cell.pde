import java.awt.Point;

class Cell
{
  int x, y, type;
  Cell(int x, int y, int type)
  {
    this.x = x;
    this.y = y;
    this.type = type;
  }
  Cell(int x, int y)
  {
    this.x = x;
    this.y = y;
  }
}
